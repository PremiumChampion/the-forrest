{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "nrf-connect",
			"request": "launch",
			"name": "Launch nb-iot\\build",
			"config": "${workspaceFolder}/nb-iot/build",
			"runToEntryPoint": "main",
			
			"preLaunchTask": "nRF Connect: Build: nb-iot/build (active)"
		}
	]
}